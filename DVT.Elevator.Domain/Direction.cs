namespace DVT.Elevator.Domain
{
    /// <summary>
    ///  An enum representing the direction an elevator is moving (Up, Down, Idle).
    /// </summary>
    public enum Direction
    {
        Up,
        Down,
        Idle
    }
}
